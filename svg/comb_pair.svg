<svg xmlns="http://www.w3.org/2000/svg" width="700" height="660">
            <style>
                .search-match {
                    stroke: #ff00ff !important;
                    stroke-width: 4px !important;
                    stroke-dasharray: none !important;
                    animation: pulse 0.75s infinite;
                }
                @keyframes pulse {
                    0% { stroke-opacity: 1; }
                    50% { stroke-opacity: 0.4; }
                    100% { stroke-opacity: 1; }
                }
            </style>
        <rect x="0" y="0" width="700" height="660" fill="rgb(255,255,255)"></rect><rect x="0" y="0" width="680" height="640" fill="rgba(255,255,255,1)"></rect><rect x="232" y="253" width="40" height="20" fill="rgba(230,230,230,1)"><title>c1</title></rect><text x="252" y="263" text-anchor="middle" alignment-baseline="middle" fill="black">+</text><rect x="276" y="253" width="40" height="20" fill="rgba(230,230,230,1)"><title>c2</title></rect><text x="296" y="263" text-anchor="middle" alignment-baseline="middle" fill="black">+</text><circle cx="240" cy="196" r="5" fill="rgb(101,67,33)"></circle><line x1="262" y1="258" x2="286" y2="258" stroke="red" stroke-width="1" />
            <foreignObject x="10" y="10" width="250" height="40">
                <div xmlns="http://www.w3.org/1999/xhtml">
                    <input type="text" 
                           id="svg-search" 
                           placeholder="Search hover text..." 
                           style="position: fixed; width: 100%; padding: 5px; border: 2px solid #333; border-radius: 4px; font-family: sans-serif; opacity: 0.9;"
                           oninput="performSearch(this.value)" 
                    />
                </div>
            </foreignObject>
        
            <script><![CDATA[
                function performSearch(query) {
                    // Normalize query
                    query = query.toLowerCase();
                    
                    // Get all shapes (rects and circles)
                    // We look for any element that has a <title> child
                    const shapes = document.querySelectorAll("rect, circle");
                    
                    shapes.forEach(shape => {
                        const titleEl = shape.querySelector("title");
                        
                        // Clear previous highlight
                        shape.classList.remove("search-match");
                        
                        if (query.length > 0 && titleEl) {
                            const hoverText = titleEl.textContent.toLowerCase();
                            if (hoverText.includes(query)) {
                                shape.classList.add("search-match");
                            }
                        }
                    });
                }
            ]]></script>
        </svg>